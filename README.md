# Nekos File Sharing Protocol (NFSP)
A encrypted file sharing protocol that supports both P2P and Client & Server communication.

## WHEN TESTING W/O INSTALL SET THE -k AND -c CORRECTLY IN BOTH THE CLIENT AND SERVER (sets the certificate and key file)